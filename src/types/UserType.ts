export type UserType = {
    id?: string,
    email: string;
    password: string;
    balance?: string | number,
    token?: string,
    firstname?: string,
    lastname?: string,
    fathername?: string,
    avatarUrl?: string
}
